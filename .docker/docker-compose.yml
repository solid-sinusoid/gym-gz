version: '3.0'

services:

  base:
    build:
      args:
        from: ubuntu:focal
      context: .
      dockerfile: base.Dockerfile
    image: diegoferigo/gym-gz:base

  # ======
  # MASTER
  # ======

  ci-master:
    build:
      args:
        from: diegoferigo/gym-gz:base
        gazebo_codename: dome
      context: .
      dockerfile: cicd-master.Dockerfile
    image: diegoferigo/gym-gz:ci-master

  pypi-master:
    build:
      args:
        from: diegoferigo/gym-gz:base
        gazebo_codename: dome
      context: .
      dockerfile: cicd-master.Dockerfile
    image: diegoferigo/gym-gz:pypi-master

  # =====
  # DEVEL
  # =====

  ci-devel:
    build:
      args:
        from: diegoferigo/gym-gz:base
        gazebo_codename: fortress
        CMAKE_BUILD_TYPE: Debug
      context: .
      dockerfile: cicd-devel.Dockerfile
    image: diegoferigo/gym-gz:ci-devel

  pypi-devel:
    build:
      args:
        from: diegoferigo/gym-gz:base
        gazebo_codename: fortress
        CMAKE_BUILD_TYPE: Release
      context: .
      dockerfile: cicd-devel.Dockerfile
    image: diegoferigo/gym-gz:pypi-devel

  # ====
  # DEMO
  # ====

  latest:
    build:
      args:
        from: diegoferigo/gym-gz:pypi-master
        branch: master
      context: .
      dockerfile: Dockerfile
    image: diegoferigo/gym-gz:latest

  nightly:
    build:
      args:
        from: diegoferigo/gym-gz:ci-devel
        branch: devel
        pip_options: --pre
      context: .
      dockerfile: Dockerfile
    image: diegoferigo/gym-gz:nightly
